{
  "name": "fetch-timeout",
  "version": "1.0.0",
  "description": "Test node-fetch lib timeout",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start:server": "node --es-module-specifier-resolution=node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "nalon",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
